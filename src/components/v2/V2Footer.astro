---
interface Props {
  context?: string;
}

const { context = 'default' } = Astro.props;
---

<footer class="mt-8 border-t border-slate-800/80 pt-6 text-[0.75rem] text-slate-400">
  <div class="flex flex-col items-start justify-between gap-3 sm:flex-row sm:items-center">
    <p>
      {context === 'blog' ? 'Writing by' : context === 'store' ? 'Built and used by' : 'Built by'} <span class="text-teal-200">Dipankar Shaw</span>. 
      {context === 'blog' ? 'Built to be fast and distraction‑light.' : context === 'store' ? 'These tools evolve as my own stack evolves.' : 'Full stack developer, focused on fast and intentional products.'}
    </p>
    <a href="/v2#contact" class="inline-flex items-center gap-1 text-teal-300 hover:text-teal-200">
      <span>{context === 'store' ? 'Ask about a bundle' : 'Say hi'}</span>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M5 12h14" stroke-linecap="round" stroke-linejoin="round"></path>
        <path d="m13 6 6 6-6 6" stroke-linecap="round" stroke-linejoin="round"></path>
      </svg>
    </a>
  </div>
</footer>
